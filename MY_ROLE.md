# 개인 기여도 보고서

본 문서는 `ai-bookcover-generator-service` 프로젝트에서 **유혁재**가 담당한 핵심 역할, 기술적 문제 해결 과정, 그리고 이를 통해 달성한 정량적 성과를 기록한 기술 명세서입니다.

---

## 담당 역할 및 핵심 요약
* **담당 역할**: AI 이미지 생성 API 파이프라인 구축 및 프론트엔드 아키텍처 설계
* **담당 문서**: `BookDetailPage.jsx`, `CoverImageGenerator.jsx`, `api/openai.jsx`
* **핵심 성과**: 
  * OpenAI API 연동 및 맞춤형 다중 프롬프트 엔지니어링 아키텍처 설계
  * 비동기 병렬 처리(`Promise.all`) 파이프라인 도입으로 체감 API Latency 대폭 단축
  * 컴포넌트 3계층 모듈화 리팩토링을 통한 유지보수성 및 협업 효율 극대화

---

## My Tech Stack
* **AI & API**: OpenAI API (gpt-image-2)
* **Frontend**: React, JavaScript, Fetch API
* **Mock Backend**: JSON Server (Mock API 구축)
* **Environment**: Node.js, npm

---

## 핵심 문제 해결 과정

### Challenge 1: 다중 이미지 생성 시의 품질 획일화 및 API 병목 현상 해결
* **Situation (상황)**: 사용자 경험(UX) 향상을 위해 3장의 표지 후보를 제공하고자 했습니다. 초기에는 API 파라미터(`n=3`)만 수정하여 구현했으나, 원본 프롬프트가 동일해 3장 모두 획일적이고 유사한 이미지가 출력되는 문제가 발생했습니다.
* **Task (과제)**: 각기 다른 분위기의 다채로운 표지 후보 3장을 생성하면서도, 3번의 호출로 인해 3배 길어지는 대기 시간 문제를 동시에 해결해야 했습니다.
* **Action (해결 활동)**: 
  * **독립적 다중 프롬프트 설계**: `n=1`로 통제하되, 기존 프롬프트에 '독창적', '전통적', '감각적' 등 상반된 스타일 지시어를 결합하여 3개의 독립적인 프롬프트를 동적으로 생성했습니다.
  * **비동기 병렬 처리 파이프라인 구축**: 3번의 직렬 API 호출로 인한 UX 저하를 막기 위해, JavaScript의 **`Promise.all`을 활용한 비동기 병렬 처리 통신 구조**를 전면 도입했습니다.
* **Result (성과)**: 
  * 단 한 번의 요청으로 서로 다른 다채로운 매력의 표지 이미지 3장을 성공적으로 제공했습니다.
  * 3장의 이미지를 렌더링함에도 불구하고 **단일 이미지 생성 시간과 동일한 수준으로 응답 시간을 최적화**하여 시스템 안정성을 향상시켰습니다.

### Challenge 2: API 통신 불안정성 극복 및 에러 추적 파이프라인 구축
* **Situation (상황)**: 팀원 간 API 연동 테스트 중 원인 불명의 이미지 생성 실패 오류가 빈번하게 발생했습니다. 하지만 초기에는 '생성 실패'라는 단편적인 결과만 반환되어 시스템 에러인지, 네트워크 문제인지 파악하기 어려웠습니다.
* **Task (과제)**: 통신 실패의 정확한 원인을 즉각 식별하여 디버깅 효율을 높이고, 불완전한 데이터로 인해 프론트엔드 화면이 렌더링 단계에서 뻗는 현상을 방지해야 했습니다.
* **Action (해결 활동)**: 
  * **원천 에러 메시지 추출 및 시각화**: `Response.ok`를 통해 호출 성공 여부를 1차 검증하고, 예외 발생 시 `await r.json()`으로 OpenAI의 내부 에러 메시지(`error?.message`)를 직접 추출해 UI에 즉각 시각화했습니다.
  * **방어적 프로그래밍(Defensive Programming) 적용**: Base64 변환 시 `b64_json` 빈 값 검증 로직을 추가해 깨진 이미지의 반환을 원천 차단하고, DB(`db.json`)로 업데이트하는 `PATCH` 통신 구간에도 검증 로직을 배치했습니다.
* **Result (성과)**: 
  * 구체적인 에러 메시지 노출을 통해 'API 키 오타', '폭력성으로 인한 OpenAI 보안 정책 위반' 등 다양한 원인을 즉각 식별하여 팀 단위의 이슈 해결 속도를 획기적으로 단축했습니다.

### Challenge 3: 단일 컴포넌트 **비대화**로 인한 협업 병목 해결 및 아키텍처 모듈화
* **Situation (상황)**: 초기 개발 단계에서는 도서 상세 정보 렌더링, AI 표지 생성 로직, 외부 API 호출부가 하나의 컴포넌트(`BookDetailPage.jsx`)에 집중되어 코드가 무거워지는 'Monolithic' 구조를 띠고 있었습니다.
* **Task (과제)**: 에러 발생 시 추적이 어렵고, 여러 팀원이 하나의 파일을 동시에 수정하면서 발생하는 협업 병목(Merge 충돌)을 방지하기 위해 코드 구조를 체계적으로 분리해야 했습니다.
* **Action (해결 활동)**: **관심사의 분리 원칙**을 적용하여 프론트엔드 아키텍처를 3개의 독립적인 모듈로 전면 리팩토링했습니다.
  * **UI 및 렌더링 계층**: `BookDetailPage.jsx` (뷰 렌더링 전담)
  * **비즈니스 로직 계층**: `CoverImageGenerator.jsx` (AI 상태 관리 및 사용자 이벤트 제어)
  * **네트워크 통신 계층**: `api/openai.jsx` (무거운 API 호출 및 에러 핸들링 전담)
* **Result (성과)**: 
  * 각 모듈의 역할이 명확해짐에 따라 팀원들이 파일을 나누어 병렬 개발을 진행할 수 있게 되어 Git 병합 충돌 없이 협업 효율이 극대화되었습니다. 
  * UI 에러와 통신 에러가 물리적으로 분리되어 유지보수성이 대폭 향상되었습니다.

---

## 배운 점 및 회고
* **엔지니어링 마인드셋의 확장**: 기능 구현에만 몰두하는 것을 넘어, **비동기 통신을 통한 성능 최적화(Latency 단축), 철저한 에러 핸들링을 통한 시스템 안정성 확보, 그리고 아키텍처 모듈화를 통한 협업 효율 증대**라는 소프트웨어 공학의 3가지 핵심 가치를 깊이 있게 체감했습니다.
* **직무 적합성**: AI 기술(생성형 모델)이 프론트엔드 및 데이터베이스와 유기적으로 통신할 때 데이터 흐름을 안전하게 제어하는 엔지니어링 감각을 길렀으며, 이는 향후 **AI 서비스 플랫폼 구현 및 고도화** 과정에 즉시 전력으로 기여할 수 있는 탄탄한 밑거름이 될 것입니다.
